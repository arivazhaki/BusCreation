import { React } from 'react';

const Mirror = () => {
	const style = {
		width: '3vw',
		height: '11vW',
		backgroundColor: 'lightgrey',
		borderRadius: '10px',
		top: '23%',
		right: '37%',
		border: '8px solid black',
		position: 'absolute',
	};

	return (
		<div style={ style }/>
	);
};

export default Mirror;
